{
  "id": "http2-pro",
  "protocol_version": "0.2.0",
  "identity": {
    "name": "HTTP2.Pro",
    "purpose": "Test endpoints for client and server HTTP/2 protocol support"
  },
  "offers": [
    {
      "intent": "use_api",
      "description": "가상의 기본 엔드포인트: https://http2.pro/doc/api",
      "endpoint": "/",
      "method": "GET",
      "side_effect": "read-only",
      "auth_type": "api_key",
      "confidence_score": 0.5
    }
  ],
  "needs": [
    {
      "intent": "identity.api_key",
      "criticality": "required"
    }
  ]
}